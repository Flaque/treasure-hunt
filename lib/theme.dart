import 'package:flutter/material.dart';

class TreasureTheme {
  static ThemeData data = new ThemeData(
      primaryColor: const Color(0xff0097E6),
      primaryColorDark: const Color(0xff2F3640),
      backgroundColor: Colors.white,
      canvasColor: Colors.white,
      brightness: Brightness.light);
}
